doctrine:
    dbal:
        url: '%env(DATABASE_URL)%'
    orm:
        auto_mapping: true
        mappings:
            App:
                is_bundle: false
                type: attribute
                dir: '%kernel.project_dir%/Entity'
                prefix: 'Pallari\GeonameBundle\Tests\App\Entity'
                alias: App
